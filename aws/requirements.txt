# 챗봇 서비스 의존성
# 베이스: vllm/vllm-openai:v0.20.2 (torch, fastapi, pydantic 등 포함)
#
# 설치: pip install -r requirements.txt && pip install --no-deps "transformers>=5.8.0"
# ※ transformers는 --no-deps 필수 (vLLM 핀 의존성 보호)
# ※ vLLM: 베이스(vllm/vllm-openai:v0.20.2)의 정식 빌드엔 Gemma 4 MTP(gemma4_mtp)가 없다.
#    → Dockerfile.llm이 wheels/의 nightly 빌드(0.20.2rc1.dev251+gef34592a1)로 덮어써서 보강한다.
#    PR #41745(Gemma 4 MTP)는 v0.22.0 first release — 정식 v0.20.2엔 미포함 (2026-06-01 실측).
#    누락 시 drafter가 draft_model로 fallback → 멀티모달 Gemma에서 NotImplementedError로 기동 실패.
# ※ FlashInfer 3종은 vLLM speculative decoding(MTP) 런타임 의존성 — 여기 핀하지 않는다.
#    vLLM/베이스 이미지가 0.6.8.post1(베이스 CUDA에 맞는 cu variant)로 제공한다 (하단 주석 참조).

# FastAPI + 서빙 (vLLM 이미지에 포함되어있으나 최소 버전 명시)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
sqlmodel>=0.0.22
jinja2>=3.1.0
sse-starlette>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
httpx>=0.27.0

# 텍스트 처리
markdown>=3.6
nh3>=0.2.0
PyYAML>=6.0.0

# LangGraph + LangChain
langgraph>=1.1.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-ollama>=0.3.0
langchain-huggingface>=0.1.0

# 검색 (ChromaDB + BM25 Hybrid)
chromadb>=0.5.0
bm25s>=0.3.0
kiwipiepy>=0.20.0

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
python-docx>=1.1.0

# AWS CLI (컨테이너 내 S3 sync 등)
awscli>=1.35.0

# RAGAS 평가
ragas>=0.4.0
datasets>=3.0.0

# STT (Voxtral / Whisper / Qwen-ASR) 오디오 의존성
# ※ 미설치 시 vLLM 기동 직후 EngineCore 가 ImportError: soundfile / soxr 로 fail.
# 모델카드 권장: pip install soxr librosa soundfile
soundfile>=0.13.0
soxr>=1.1.0
librosa>=0.11.0

# vLLM Speculative Decoding (MTP) — FlashInfer 3종은 vLLM/베이스 이미지가 제공한다(여기 핀 안 함).
# ※ vLLM nightly가 flashinfer-python/cubin을 ==0.6.8.post1로 strict 핀하고, jit-cache는 베이스
#    이미지의 0.6.8.post1(+cu13x)을 그대로 쓴다 → 셋 다 0.6.8.post1·동일 cu variant로 일관된다.
# ※ 별도로 0.6.11/cu129 등으로 재고정하지 말 것. 베이스 환경의 CUDA와 어긋나면(예: cu129 모듈을
#    cu13 베이스에 깔면) flashinfer가 libcudart.so.12를 요구하다 EngineCore가 죽는다.

# 테스트
pytest>=8.0
