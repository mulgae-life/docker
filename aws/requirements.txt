# 챗봇 서비스 의존성
# 베이스: vllm/vllm-openai:v0.19.0 (torch, fastapi, pydantic 등 포함)
#
# 설치: pip install -r requirements.txt && pip install --no-deps "transformers>=5.5.0"
# ※ transformers는 --no-deps 필수 (vLLM 핀 의존성 보호)

# FastAPI + 서빙 (vLLM 이미지에 포함되어있으나 최소 버전 명시)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
sqlmodel>=0.0.22
jinja2>=3.1.0
sse-starlette>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
httpx>=0.27.0

# 텍스트 처리
markdown>=3.6
nh3>=0.2.0
PyYAML>=6.0.0

# LangGraph + LangChain
langgraph>=1.1.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-ollama>=0.3.0
langchain-huggingface>=0.1.0

# 검색 (ChromaDB + BM25 Hybrid)
chromadb>=0.5.0
bm25s>=0.3.0
kiwipiepy>=0.20.0

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
python-docx>=1.1.0

# AWS CLI (컨테이너 내 S3 sync 등)
awscli>=1.35.0

# RAGAS 평가
ragas>=0.4.0
datasets>=3.0.0

# 테스트
pytest>=9.0.0
