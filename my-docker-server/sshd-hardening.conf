# 브루트포스 대응 (cfd / dev-fullstack 컨테이너 sshd 공통)
# - 미인증 좌석(MaxStartups)을 넉넉히 두고, 좌석 점유 시간(LoginGraceTime)은 짧게
# - 연결당 비밀번호 시도 횟수 제한, IP당 동시 미인증 연결 제한
# - AllowUsers 는 Dockerfile 에서 ${USERNAME} 으로 뒤에 붙인다
MaxStartups 30:50:100
LoginGraceTime 30
MaxAuthTries 3
PerSourceMaxStartups 3
